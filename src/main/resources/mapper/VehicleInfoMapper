<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.face.hotel.mapper.VehicleInfoMapper">

    <select id="getHours" resultType="java.lang.String">
        SELECT
            time_in
        FROM
            vehicle_info
        WHERE
            user_id = #{userId}
    </select>

    <select id="selectLastVehicleIn" resultType="com.face.hotel.entity.VehicleInfo">

    </select>

    <select id="selectAllVehicleInfo" resultType="com.face.hotel.entity.VehicleInfo">

    </select>

</mapper>